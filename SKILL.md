---
name: wechat-cover-studio
description: 根据产品 Logo、主副标题和可选背景参考，生成或局部修改 2.35:1 公众号封面；内含用户认可的案例、原始提示词与正反例，适用于案例展示、产品评测、模型发布封面和封面提示词编写。
---

# 公众号封面工作室

把案例或主题画面作为主视觉，配合准确的品牌识别与少量有层次的文字，直接交付成图。用户只要求提示词时，交付可直接使用的完整提示词，不擅自生成。用户当次要求优先于本技能默认值。

## 输入与背景选择

通常输入：产品 Logo、主标题、副标题；可选背景图、视觉元素、背景方向、希望保留/改变的部分。
推荐用户提供与主题或产品有关的视觉元素（宣传图、局部图、案例截图、电影画面），或一句方向描述。这是这些案例成功的重要经验，不是硬性前置条件。已有足够输入就开始，不为可选背景反复追问。
例如原话：“背景图可以做成一个蓝色系的右边是一条鲸鱼的背景”，已经足够指导色系、主体、位置。

先辨别参考用途，按用户措辞处理：
- “就用这张背景/其他别改”：保留实际主体、效果、气氛，适配横幅与文字；不随意重构作品。
- “参考这里的元素”：提取指定形状、物件、构图或氛围并重新设计，不把参考中的文字、按钮等照搬。
- 只有文字方向：把色系、主体、位置扩展成完整场景、光线、材质和排版。
- 无背景方向：依产品/标题语义自主选一个有辨识度的视觉概念，可用真实摄影、艺术作品、电影场景或品牌装置。避免所有产品都套黑橙 Coding 或办公室。
- 左右对比：保留指定的两幅/局部及其顺序和可辨差异，不擅自“美化”比较对象。重新安排文字避免遮挡。
信息不足以确定准确文案或 Logo 身份时才询问；不伪造 Logo。用户不需要 Logo 或副标题时允许省略。

## 先选案例，再设计

每次先读 [案例索引](references/cases.md)，选择最相关的 1–2 个案例；打开对应正例图，并读该案例链接的原始提示词及修正。不要把全部提示词与图片一股脑作为生成参考。
字号拿不准或做文字修改时，同时打开相关正反例对照。图片均保存在 references/images/，用于学习风格，不是新任务默认背景或品牌素材。
需要写新提示词时读 [提示词构造与执行](references/prompt-workflow.md)。
需要理解历史优先级或冲突时读 [反馈与验收](references/feedback.md)。完整历史见 references/source-conversation.md，仅按需查阅。

## 核心审美与排版

- 默认最终宽高比 2.35:1。生成工具未支持时使用可用横幅尺寸，再按其允许的编辑流程扩展/裁切到准确比例；不可横向拉伸。成图应核实实际像素比例，不把提示词写了比例当作完成。
- 默认左文右图；右文左图（Qwen）、左上小标题（Kimi）、对比拼图同样有效，明确布局要求优先。
- 背景负责视觉吸引力。文字应突出但克制，并在公众号缩略展示下可读；不用统一的标题宽度百分比。长标题优先单行，通过字号和空间分配处理；用户明确不换行时必须单行。
- 主副标题保持层次。字距、适度渐变、关键词着色、衬线/笔刷或一条细线可按案例选用，不能增加效果时连带把字号放大。
- 默认新增文字仅有指定主副标题，及 Logo 自带且用户要求保留的字标。不擅加品牌卡片、英文口号、角标、卖点小字、参数说明或重复品牌。真实背景产品包装文字可保留；参考网页 UI、旧日期或说明文字不默认继承。
- Logo 忠于原始形状与比例。根据要求选“仅图标”或“完整标志”，去除无关底板，避免与标题重复。不要擅自将标志装置化；用户提供/要求品牌装置时才沿用其视觉结构。
- “不要 AI 味”不是禁止科技元素。深蓝、电光、蜂巢、轨道、界面、机器人均可在主题和参考支持下使用。避免通用元素堆砌、单薄材质、全屏泛光。真实饮料广告的蓝白不属于一概禁用范围。
- 配色鲜明、有明暗层次，主体清楚，留白服务文字。不是一律深色；Wan 的暖纸白、MiniMax 的明亮海水都有效。
- 主题体现要自然。未经提供的虚构背景不得称为用户产品的真实生成案例。

## 生成、修改与交付

使用当前环境可用的图像生成/编辑能力，并遵守该能力的指令。若环境提供 imagegen 技能，执行生图时读取其说明。不要为本技能假定私有接口、固定模型或密钥；没有生图能力时说明限制并提供完整提示词，不声称已生成。
先查看当前提供的 Logo/背景；将本次实际素材作为参考输入。历史案例仅在对风格有帮助时选取，明确“学习排版/光线，不复制其中品牌、人物、文案”。
生成后实际查看图片，并按以下项目检查：
1. 主副标题逐字准确（尤其中文形近字、模型数字、英文大小写、标点）。
2. 无额外文案、重复品牌、未经要求的小卡片；Logo 无变形。
3. 字号既不细小单薄，也不压过主视觉；在缩略尺寸仍有辨识度。
4. 对齐、留白、主体裁切、最终比例符合要求。
5. 与当次背景方向和所选案例的质感一致。
出现缺陷先做局部修正。用户只改字号/错字/一句文案时，尽量固定背景、主体、颜色、构图与其他文字。必要时选择当前工具支持的精确合成方式，不把生成模型对 Logo/文字的复刻当成保证。修复后再次目视确认。
正常直接交付一张最合适的封面；用户要多方向时，改变视觉概念、材质或场景，不只换色。若连续两次针对同一问题的修正仍未解决，说明具体缺陷和可用替代方案，不无限抽卡或声称通过。
交付显示成图及可下载文件；简短说明必要的限制。未经要求不附长篇设计论证。

